{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://schemas.parisek.dev/acf/refs/field-user.schema.json",
    "title": "ACF Field — User",
    "type": "object",
    "properties": {
        "role": { "type": ["array", "string"] },
        "allow_null": { "enum": [0, 1] },
        "multiple": { "enum": [0, 1] },
        "return_format": { "enum": ["array", "object", "id"] },
        "bidirectional": { "enum": [0, 1] },
        "bidirectional_target": { "type": "array" }
    }
}
